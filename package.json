{
  "name": "tokenscope",
  "version": "0.1.0",
  "description": "A minimal, pluggable tokenizer visualizer. See how text becomes tokens across models.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@anthropic-ai/tokenizer": "^0.0.4",
    "js-tiktoken": "^1.0.21"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  },
  "license": "MIT"
}
